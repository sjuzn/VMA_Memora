package sk.upjs.druhypokus.adapters

import androidx.recyclerview.widget.RecyclerView
import sk.upjs.druhypokus.entity.Milestone

class MilestonesAdapter{ /*(private val listener: OnWeblinkClickListener) :*/
 /*   RecyclerView.Adapter<MilestonesAdapter.WeblinksViewHolder>() {

    var cachedWeblinks: List<Milestone> = emptyList()
        set(value) {
            field = value
            notifyDataSetChanged()
        }

    override fun onBindViewHolder(holder: WeblinksViewHolder, position: Int) {
        holder.bind(cachedWeblinks[position], listener)
    }

    override fun getItemCount() = cachedWeblinks.size
*/
}